# NewsFeed
Angularjs app on spring server for reading rss feeds

# Instalation
###Database
Make sure you have a PostGres DB running and configure the settings in the application.properties file

###Run using spring boot
Start the server via spring boot: mvn spring-boot:run
A local server will be created at http://localhost:8080
